"
I am a possibly (partially) unscraped message. I can contain only partially initialized fields.
"
Class {
	#name : #DDUnscrapedMessage,
	#superclass : #DDMessage,
	#category : #'DiscOrDance-Model-Base'
}
